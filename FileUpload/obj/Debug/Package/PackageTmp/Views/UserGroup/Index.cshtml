@model IEnumerable<FileUpload.Models.UserGroup>

@{
    ViewBag.Title = ViewBag.Title;
}

<h3 style="margin-left:10px;">@ViewBag.Title</h3>

<p class="distance">    
    <a href="@Url.Content("~/UserGroup/Create")" class="btn btn-success" style="margin-left:10px;">Add Group</a>
</p>
<table class="table table-striped distance" id="tbl">
    <thead>
        <tr>
            <th>Group Name</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.UserGroupName)
                </td>
                <td>
                    <a href="@Url.Content("~/UserGroup/Edit/" + item.intMstUserGroup )" >Edit</a> |
                    <a href="@Url.Content("~/UserGroup/Delete/" + item.intMstUserGroup )" >Delete</a>
                </td>
            </tr>
        }
    </tbody>

</table>

@section scripts
{
    <script type="text/javascript">

        $(function () {

            table = $("#tbl").dataTable({                
                "bPaginate": true,
                "bLengthChange": false,
                "bFilter": true,
                "bSort": false,
                "bInfo": false,
                "oTableTools": {
                    "sRowSelect": "single",
                    "sSelectedClass": "selected"
                }
            });

        });
           

    </script>    
        
}