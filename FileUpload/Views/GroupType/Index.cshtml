@model IEnumerable<FileUpload.Models.GroupType>

@{
    ViewBag.Title = "Group Type";
}

<h3 style="margin-left:10px;">@ViewBag.Title</h3>

<p class="distance">    
    <a href="@Url.Content("~/GroupType/Create")" class="btn btn-success" style="margin-left:10px;">Add Group Type</a>
</p>
<table class="table table-striped distance" id="tbl">
    <thead>
        <tr>
            <th>Group Type</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.GroupTypeName)
                </td>
                <td>
                    <a href="@Url.Content("~/GroupType/Edit/" + item.intMstGroupType )" >Edit</a> |
                    <a href="@Url.Content("~/GroupType/Delete/" + item.intMstGroupType )" >Delete</a>
                </td>
            </tr>
        }
    </tbody>

</table>

@section scripts
{
    <script type="text/javascript">

        $(function () {

            table = $("#tbl").dataTable({                
                "bPaginate": true,
                "bLengthChange": false,
                "bFilter": true,
                "bSort": false,
                "bInfo": false,
                "oTableTools": {
                    "sRowSelect": "single",
                    "sSelectedClass": "selected"
                }
            });

        });
           

    </script>    
        
}